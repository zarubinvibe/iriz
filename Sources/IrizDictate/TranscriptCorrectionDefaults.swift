// Заводской словарь замен для режима вставки. Это отдельный набор правил:
// prompt-режим имеет другую семантику и не должен делить с ним глоссарий.
//
// Каждая запись сопоставляется как точная словоформа с границами слова.
// Новые правила принимаются только вместе с синтетической регрессией.
import Foundation

/// Имена продуктов, устойчивые ошибки распознавания и закрытый список
/// общеупотребимых аббревиатур для обычной диктовки.
// Заводской словарь раздаётся всем и вкомпилирован в бинарник, поэтому личного
// в нём нет: ни имён частных проектов владельца, ни записей о том, как
// распознавание расслышало лично его. Своё он добавляет себе сам — редактором
// словаря, в свои настройки, и наружу это не уезжает.
public let defaultTranscriptCorrections: [TranscriptCorrection] = [
    // MARK: имя продукта
    //
    // Продукт называется Иридой с 03.09.2026. Прежние пары оставлены: владелец
    // произносил старое имя больше года, и выкинуть их значит сломать
    // распознавание его собственной привычной речи раньше, чем она сменится.
    // Заводской словарь дописывается, а не переписывается - у владельца уже
    // есть свои правки поверх него.
    ("smalltalk", "iriz"),
    ("смолток", "iriz"),
    ("ирида", "iriz"),
    ("ириду", "iriz"),
    ("ириды", "iriz"),

    // MARK: Модели
    ("клод", "Claude"),
    ("клода", "Claude"),
    ("кими", "Kimi"),

    // MARK: Устойчивые ошибки распознавания
    ("гидхаб", "гитхаб"),
    ("нещатно", "нещадно"),
    ("промп", "промпт"),

    // MARK: Закрытый список аббревиатур
    ("эцп", "ЭЦП"),
    ("омвд", "ОМВД"),
    ("гибдд", "ГИБДД"),
    ("мвд", "МВД"),
    ("ндс", "НДС"),
    ("ип", "ИП"),
].map { TranscriptCorrection(source: $0.0, replacement: $0.1) }
