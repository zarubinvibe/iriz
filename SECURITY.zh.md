# 安全

[English](SECURITY.md) · [Русский](SECURITY.ru.md)

<p align="center"><img src="docs/assets/pantheon/window-wash-source.png" alt="白色大理石的伊里斯在古典柱旁擦拭玻璃：一眼看穿，什么都没藏" width="100%"></p>

## 这里真正值钱的是什么

你的语音和你的文字。不是密码，也不是支付信息——但对律师、医生或心理咨询师来说，这些比密码更贵：
里面有姓名、金额、诊断，以及别人的秘密。

所以界线画在看得见的地方：

- **声音不会离开这台机器。** 识别在你的 Mac 上完成。口述期间，网络被识别库内部的一个开关关掉，
  即使下载失败，这个开关也会回到原位。
- **唯一一次联网**是下载识别模型，而且由你按下按钮才开始。
- **只有提示词模式会把转写送出去**，只送给你选定的代理，而且只在你按下它的按键时。Ollama 选项
  什么都不发送：它在这台 Mac 上计算。
- **口述记录以明文存在你的磁盘上**，权限为 `0600`。磁盘加密是 FileVault 的职责，不是我们的：
  在系统加密之上再承诺一套自己的加密，等于承诺两次、一次都做不到。

## 这些是怎么被验证的，而不是被口头保证的

README 里的承诺，在工具踢它一脚之前一文不值：

```bash
bash scripts/offline_binary_gate.sh   # 二进制里的网络符号，运行进程的套接字
bash scripts/cleanup_privacy_gate.sh  # 口语清理不会泄漏到别人手里
bash scripts/privacy_tracked_data_gate.sh
bash scripts/verify.sh                # 全部一起：测试和闸门
```

一旦承诺和代码不一致，闸门就会变红。这才叫验证。

## 如果你发现了漏洞

如果这个漏洞不会立刻让人拿到别人的数据，请开 Issue。如果会，请私下告诉我，不要公开：
`7teenno1@gmail.com`。你会收到回复，而且是实质性的回复。

请不要把你自己的口述记录附在报告里。描述路径就好——我会用我自己的复现。

## 这个项目不承诺什么

- 不承诺能防住已经用你的账户坐在你 Mac 前的人。
- 不承诺 Apple 公证：构建使用自签名证书，macOS 会在首次启动时如实警告。
- 不承诺第三方代理在提示词模式下会规矩。它是第三方。正因如此，提示词模式默认关闭，由你打开。

## 为什么这条界线画在这里

我是律师。我口述的内容里有客户的名字、金额、案情，还有法庭上说过的话。这些东西一旦离开我的
机器，我就再也控制不了它去了哪里、被谁看过、留存多久。这不是抽象的担心：对律师、医生、心理
咨询师来说，一次泄漏就足以毁掉信任关系本身。

云端口述的识别质量确实更好。但代价是把上面这些内容交出去。所以这个应用做了取舍：识别放在本机，
质量差一点，换来的是声音根本没有出去过。

## 出问题时会发生什么

- **下载模型失败。** 网络开关会回到关闭位置，不会因为出错就一直开着。这一点有测试守着。
- **粘贴没成功。** 文字不会消失，它留在浮窗展开的面板里，你可以拷贝或者再粘贴一次。
- **应用崩溃。** 已经写到磁盘上的口述记录还在原处。原始转写在任何检查之前就已落盘。

## 你可以自己检查什么

不用相信这一页。构建之后自己跑一遍上面那几条命令：闸门会告诉你二进制里有没有网络符号，运行中的
进程有没有打开套接字。如果闸门变红而这一页说没问题，那是这一页错了，请开 Issue。
