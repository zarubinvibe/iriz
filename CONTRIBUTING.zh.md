# 参与进来

[Русский](CONTRIBUTING.ru.md) · [English](CONTRIBUTING.md)

<p align="center"><img src="docs/assets/pantheon/doc-contributing.png" alt="矮桌上并排放着两块一模一样的大理石板，左边那块上落着金色的声音丝带，右边那块空着等人来写，旁边立着传令杖" width="100%"></p>

那张图里右边的石板是故意空着的。这里给别人的活留了位置，而且和给我自己的一样大。

## 你需要什么

一台 macOS 14 或更新的 Mac，加上带 Swift 6 的 Xcode。别的不用：这个项目只有一个依赖，它自己会拉下来。

```bash
git clone https://github.com/zarubinvibe/iriz.git ~/iriz
cd ~/iriz
bash install.sh
```

不带参数的时候，安装脚本什么都不装。它只看你的机器缺什么，跑一遍目录自检，然后告诉你下一步该做什么。真要编译并放进 `/Applications`，是后面那条 `bash install.sh --build`。

## 提交之前

```bash
swift test
bash scripts/selfcheck.sh --selftest
```

测试必须全绿。改动如果动了行为，就带上一个改之前会失败的测试：不然过一个月，谁也分不清那是修好了还是碰巧。

## 这里的代码怎么写

能用的最简单做法。标准库或者系统本身的能力，排在你自己造的抽象前面；一行能解决的，不写一个类。抽象是等到第二个需求出现才建的，不是第一个。

注释解释的是「为什么」，不是「做了什么」。`// 计数加一` 对谁都没用；写清楚哪个故障是实际撞上的、哪天撞上的，能给下一个人省一天。

## 一次改动的路线

fork、开分支、提交、push、Pull Request。没有人直接往 `main` 推。提交信息写清楚改了什么、为什么改，不要写「修复若干」。

发现坏了但不想自己动手？开一个 Issue，写下你按了什么、屏幕上出现了什么、你本来以为会出现什么。这样一份记录比一个补丁更值钱：它显示的是产品在活人手里是怎么坏的。

## 现在最缺的是什么

应用界面目前只有俄文，文档已经有三种语言了。界面的本地化是最大的一块没做完的活。再往后是自己画的图标，用来替掉系统那一套；还有在 Intel 的 Mac 上真跑一次：二进制里带着那一片，可作者手上没有那台机器。
